/**
 * 활동 시간(ActivityTime)에 대한 enum 정의
 * 각 값은 한국어 문자열로 매핑됨
 */
export enum ActivityTime {
  MIDNIGHT = '밤', // 자정 시간
  MORNING = '아침', // 아침 시간
  AFTERNOON = '오후', // 오후 시간
  EVENING = '저녁', // 저녁 시간
}
